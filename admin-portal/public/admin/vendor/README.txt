supabase.js is supabase-js version 2.117.2 (MIT licence), from
https://github.com/supabase/supabase-js — the browser build (dist/umd/supabase.js).

It is kept here, next to the portal, so the portal loads no code from the
internet. The portal's security policy allows scripts from this computer only.
